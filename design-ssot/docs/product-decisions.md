# 프로덕트 결정 기록 (Product Decisions)

> 게이트 조건 G1. CTO 해결사 문서의 P1·P2를 코디네이터가 확정·기록. 2026-09-05.

## P1 · 가격: $8 채택 (권고 수용)

- 근거: 설계 문서(09-03) 확정값 $8 · 사업화 논의(09-05)도 $8 유료 처방 합의 · 경쟁 실측상 단건 심리 가격대 유효.
- 모든 화면·카피·문서의 가격 표기는 `{{PRICE}}` 토큰으로 유지한다(하드코딩 금지 · G1 검증 대상).
- $12 표기는 flows-verification 초기 오기로 판정, 전면 폐기.

## P2 · 서비스명: {{BRAND}} 토큰 유지 선언 (확정 연기)

- 후보 SajuRoot(상표 검토 통과)가 유력하나 최종 확정 전까지 **{{BRAND}} 토큰**으로 모든 화면·카피·공유 카드 시그니처를 유지한다.
- 연기 비용(CTO 리스크 #3): 공유 카드 시그니처 밴드가 토큰 채움 상태로 남음.
- **확정 데드라인: 프로덕션 화면 재생성 착수 전(Stitch U1 라운드 이후 즉시).** 미확정 시 SajuRoot를 기본 채택하는 절차로 자동 승격한다.
- 도메인 4종 등록 여부는 본 결정과 함께 확인 필요(아카이브 O3).

## 토큰 규칙 (G1 검증 대상)

`{{BRAND}}` · `{{PRICE}}` 외 하드코딩 금지 범위: design-ssot/02-direction, design-ssot/04-design-tokens 내 가격·서비스명 문자열.

## P2 갱신 (2026-09-05, 데드라인 도래)

{{BRAND}} 확정 데드라인(U1 완료 직후)이 도래했다. product-decisions.md 절차에 따라 **SajuRoot 기본 채택(임정)**으로 자동 승격한다. {{BRAND}} 토큰 체계는 유지되며 치환값만 확정된다. 이의(변경) 시 본 문서를 수정하면 된다.
