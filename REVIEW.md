# REVIEW.md — PR 리뷰 정책 (AI-Native SDLC Gate 5)

> Anthropic Applied AI 플레이북 채택. 다층 AI 리뷰어가 diff를 승인된 plan.md·보안 정책과 대조하고, 인간 리뷰는 고위험 변경에 집중한다.

## 3대 검증 패스 (모든 PR 필수)

1. **버그 (Bug)**: 로직 오류 · 엣지 케이스 · 회귀 — 승인된 plan.md의 acceptance criteria와 대조
2. **보안 (Security)**: 시크릿 유출 · 의존성 · 인젝션 · 권한 — 특히 Pages 공개 저장소이므로 공개되면 안 되는 데이터(개인정보 · API 키 · 비공개 시안) 노출 검사
3. **컴플라이언스 (Compliance)**: 프로젝트 브랜드 법(오컬트 금지 · 운세 어휘 금지 · em-dash 금지) · 디자인 시스템 토큰 · intent/spec과의 일치

## 5-Nit Cap

AI 리뷰어의 사소한 문체·스타일 지적은 **최대 5개**. 초과 시 한 줄 요약으로 묶는다. 리뷰는 3대 패스에 집중한다.

## 인간 리뷰 집중 영역 (고위험 변경)

- 인증/인가 · 결제 · DB 마이그레이션 (해당 시)
- **공개 웹(Pages)에 노출될 콘텐츠의 최종 승인** — 시안·카피·라벨
- intent.md의 Status 전환 (draft → approved = 비즈니스 승인)
- 파이프라인 스크립트·CI 워크플로우 변경

## 브랜치 워크플로우

| 모드 | 흐름 |
|---|---|
| 단독 (기본) | `main` → 커밋 → `main:design-ssot` 푸시 (Status 전환 커밋이 게이트 기록) |
| 팀 (전환 시) | `intent/[슬러그]` PR → PO 머지 → `spec/[슬러그]` PR → Tech Lead 머지 → `feature/[슬러그]` PR → CI 통과 + 엔지니어 머지 |

## 안전ガ드레일 (변조 방지)

- 에이전트가 **실패하는 검증을 통과시키려고 검증 코드 자체를 완화하는 행위** 금지 — 에이전트는 프로덕션 산출물만 수정한다
- `agent-evals.yml`의 검증 로직 변경 PR은 인간 리뷰 필수 (고위험 변경)
